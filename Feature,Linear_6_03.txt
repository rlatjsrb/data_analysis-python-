Feature 정규화(Normalize)
- Feature를 정규화할 경우. 값의 분포를 정규 분포 형태로 변경할 수 있음
- 일반적으로 Feature 값에 대한 정규화를 수행할 경우, 더 안정적으로 머신러닝 모델을 학습시킬 수 있음

상관분석(Correlation Analysis)
- 상관 분석 또는 '상관관계' 또는 '상관'은 확률론과 통계학에서 두 변수간에 어떤 선형적 또는 비선형적 관계를 갖고 있는지를 분석하는 방법론